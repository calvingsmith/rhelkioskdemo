---
- name: Pre-flight variable validation
  hosts: all
  connection: local
  gather_facts: false
  roles:
    - { role: preflight, run_once: true }

- name: Provision gnome-shell kiosk VM
  hosts: gnome_shell_hosts
  roles:
    - gnome_shell_kiosk
