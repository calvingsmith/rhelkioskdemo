[defaults]
inventory = inventories/
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[inventory]
ignore_extensions = .pyc, .pyo, .swp, .bak, ~, .rpm, .md, .txt, .rst, .orig, .ini.bak, .example
