# Committed inventory with group structure and change-me sentinels.
# Real values come from local.ini (see local.ini.example).

[gnome_kiosk_hosts]
fedora44 ansible_host=change-me
rhel10 ansible_host=change-me

[gnome_shell_hosts]
rhel10-atop ansible_host=change-me

[all:vars]
ansible_user=cloud-user
ansible_ssh_private_key_file=change-me
