[Unit]
Description=Kiosk session focus guard (reclaim seat0 from GDM greeter if it wins the readiness race)
After=graphical-session.target

[Service]
ExecStart=%h/.local/bin/gnomekiosk-session-focus-guard.sh
Restart=always
RestartSec=1

[Install]
WantedBy=graphical-session.target
