---
# Full gnome-shell kiosk provisioning (playbook-gnome-shell.yml).
# Fast deploy uses tasks_from: app.yml, extension.yml, or all.yml via playbook-deploy.yml.

- name: Provision gnome-shell kiosk infrastructure
  ansible.builtin.import_tasks: infra.yml
