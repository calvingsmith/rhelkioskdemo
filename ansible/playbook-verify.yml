---
- name: Pre-flight variable validation
  hosts: all
  connection: local
  gather_facts: false
  roles:
    - { role: preflight, run_once: true }

- name: Verify kiosk and shell demo VMs
  hosts: gnome_kiosk_hosts:gnome_shell_hosts
  gather_facts: false
  roles:
    - kiosk_verify
